# Everything the API needs to run. The embedding classifier is optional and
# lazily loaded, so torch/transformers stay out of the process that will parse
# hostile PDFs. Install requirements-ml.txt separately if you want semantic
# categorisation locally.
fastapi==0.128.8
uvicorn==0.39.0
starlette==0.49.3
pydantic==2.13.4
pydantic-settings==2.11.0
python-multipart==0.0.20
python-dotenv==1.2.1
httpx==0.28.1
PyJWT[crypto]>=2.8.0
SQLAlchemy==2.0.52
psycopg[binary]>=3.2.0
numpy>=1.26
pytest==8.4.2
